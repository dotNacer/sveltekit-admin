---
'sveltekit-admin': minor
---

feat: **`dashboard.widgets` composes the home page from an ordered array.** `stats` renders the two global cards, `models` a grid you can title and restrict. An unknown type, or a widget pointing at a missing or excluded model, throws at boot. Omitting `dashboard` keeps the previous page.
