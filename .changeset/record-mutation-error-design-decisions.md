---
---

Docs only — nothing published changes. Records the two design decisions taken while implementing the form-repopulation PR, so the spec stops describing shapes the code deliberately rejected.
