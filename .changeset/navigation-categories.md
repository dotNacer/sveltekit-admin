---
'sveltekit-admin': minor
---

feat: **`navigation.categories` groups sidebar models under labelled headings.** Models left out of every category stay in an uncategorised tail below them. Labels and model names are validated at boot: none empty, no model in two categories, no duplicate label.
